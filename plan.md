# Plan: prj-645-task-mugqplju

**Project:** `prj-3265`  
**Task ID:** `task-mugqplju`  
**Repo:** `prj-645-task-mugqplju`  

## Summary

Декомпозиция задачи 'CLI to-do на Rust': создание структуры проекта с cargo, реализация CRUD-операций над задачами с хранением в JSON, парсинг аргументов через clap, покрытие unit-тестами, README. 4 последовательных subtask.

## Subtasks

### 1. Инициализировать cargo-проект и каркас CLI

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать Rust-проект 'rust-todo' через cargo init --bin, прописать зависимости (clap, serde, serde_json, anyhow, dirs) в Cargo.toml, настроить структуру модулей (main.rs, cli.rs, todo.rs, storage.rs) и базовый enum Cli с подкомандами add/list/done/remove.
- **Test plan:** cargo build выполняется без ошибок; cargo run -- --help выводит список подкоманд; в Cargo.toml присутствуют clap, serde, serde_json, anyhow.

### 2. Реализовать модель задачи и JSON-хранилище

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Описать структуру Task { id: u64, title: String, done: bool, created_at: String }, методы сериализации через serde. Реализовать модуль storage.rs: load/save списка задач в ~/.local/share/rust-todo/todos.json (через dirs), создание директории при отсутствии, обработка ошибок через anyhow.
- **Test plan:** Storage::save + Storage::load round-trip сохраняет и читает список задач идентично; отсутствующая директория создаётся автоматически; повреждённый JSON возвращает понятную ошибку.
- **Dependencies:** `sub-1`

### 3. Реализовать команды add/list/done/remove

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** В cli.rs реализовать обработчики: add <TITLE> — добавляет задачу с автоинкрементом id; list — печатает все задачи с id, статусом ([x]/[ ]) и заголовком; done <ID> — помечает выполненной; remove <ID> — удаляет. Все команды используют storage::load/save.
- **Test plan:** cargo run -- add 'Купить молоко' создаёт запись; cargo run -- list показывает её с [ ]; cargo run -- done 1 меняет на [x]; cargo run -- remove 1 удаляет; несуществующий id даёт понятную ошибку.
- **Dependencies:** `sub-2`

### 4. Написать unit-тесты и README

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Покрыть unit-тестами storage round-trip и логику добавления/завершения/удаления (#[cfg(test)] модули). Написать README.md с описанием, примерами команд, инструкцией cargo install --path . и путём хранения данных.
- **Test plan:** cargo test проходит все тесты зелёным; README содержит секции Usage/Install/Data Storage с примерами; cargo build --release собирает бинарник без warnings.
- **Dependencies:** `sub-3`


---
*Generated by Hermes Orchestrator*
